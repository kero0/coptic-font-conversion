[package]
name = "coptic-font-conversion"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "3.2.4", features = ["derive", "unicode"] }
regex = { version = "1.5.6", features = [ "unicode" ] }

[dev-dependencies]
rust-analyzer = "0.0.1"
rustfmt = "0.10.0"
